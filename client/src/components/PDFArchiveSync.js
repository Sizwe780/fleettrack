export const syncPDFArchive = ({ missionId, pdfData }) => {
    console.log(`📤 Syncing PDF for mission ${missionId} to employer archive`);
    alert('PDF synced to employer dashboard (simulated)');
  };